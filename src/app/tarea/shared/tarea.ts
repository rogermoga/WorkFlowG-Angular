//tarea.ts
/**
 * we define the class tarea with the fields that we are going to use
 */
export class Tarea {
    codigo: string;
    descripcion: string;
    aplicacion: string;
    tipo: string;
    estado: string;
    fechaAlta: string;
    usuario: string;
    despliegue: string;
}