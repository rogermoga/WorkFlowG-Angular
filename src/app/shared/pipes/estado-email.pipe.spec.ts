import { EstadoEmailPipe } from './estado-email.pipe';

describe('EstadoEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
